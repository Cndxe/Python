# Tableau

Voici un exemple de fonction pour trier un tableau :

```python
# Exemple de code en Python

def trier_tableau(tableau):
    return sorted(tableau)

# Exemple d'utilisation
mon_tableau = [5, 3, 8, 1, 2]
print(trier_tableau(mon_tableau))  # Sortie : [1, 2, 3, 5, 8]
```