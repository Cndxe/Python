# Les variables

###  Qu'est ce qu'une variable ?

Les variables stockent temporairement des données en mémoire. 
Il en existe 5 types (nature de variable):

- **int** pour integer --> les nombres entiers (positifs ou négatifs) sans limite de taille.
- **float** --> les Réels, nombres à virgules.
- **str** pour string --> les chaine de caractères (= mots, phrase..).
- **bool** pour boléen --> valeurs True (vrai, 1 ) ou False (faux, 0 ).
- **complex** --> représente les nombres complexe (partie réelle + partie imaginaire).
- **NoneType** --> représenté pas **None** utilisé pour indiquer l'absence de valeur.
- **list** --> liste ou tableau modifiable.
- **Tuple** --> liste ou tableau non modifiable.
- **dict** --> dictionnaire, paires de clé-valeur.





## Chaînes de caractères autrement dit "string"

### Exemple 1: Concaténation de chaînes

```python
a = "Hello "
b = "world"
result = a + b 
print(result)  # Affiche: Hello world
```

### Exemple 2: Longueur d'une chaîne

```python
mot= "Welcome"
print(len(mot))  # Affiche: 7
```

### Exemple 3: Conversion à la majuscule

```python
min = "bonjour"
print(min.upper())  # Affiche: BONJOUR
```


> [!NOTE]
> Le dépôt est en cours de création rien n'est terminé ! :) 
